# テスト実行用の依存。本番（ラズパイ）には不要。
# 本体は requests のみに依存しており、requirements.txt は置いていない。
requests>=2.31.0
pytest>=8.0.0
# 制御ループのテストは無限ループを扱うため、取りこぼすとCIが数時間ハングする。
# タイムアウトで «失敗» に変えて早期に気付けるようにする。
pytest-timeout>=2.3.0
